/// <reference types="node" />
import { SpawnOptions } from 'child_process';
import { CliResult, CancellationToken } from './types';
export declare class Cli {
    static execute(args?: string[], options?: SpawnOptions, token?: CancellationToken): Promise<CliResult>;
    private static readonly CF_LOGIN_ERROR;
    private static readonly CF_CMD;
    private static cliResultOnExit;
    private static updateSpawnOptions;
}
