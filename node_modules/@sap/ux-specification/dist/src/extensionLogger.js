"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.log = void 0;
const ux_specification_types_1 = require("@sap/ux-specification-types");
/**
 * Function to create a new log entry and add it to the logger.
 * If no logger is passed, puts the message to the console
 * @param logger - the logger instance
 * @param message - the log message
 * @param meta - option list of strings/objects
 */
const log = (logger, logEntry, ...meta) => {
    if (logger) {
        if (logEntry.location) {
            logger[logEntry.severity](logEntry.message, { location: logEntry.location, ...meta });
        }
        else {
            logger[logEntry.severity](logEntry.message, ...meta);
        }
    }
    else {
        if (logEntry.location) {
            if (logEntry.location.range) {
                console.log(`${ux_specification_types_1.LogSeverityLabel[logEntry.severity]}: ${logEntry.message}, location: ${logEntry.location.path}, ${logEntry.location.range}`, ...meta);
            }
            else {
                console.log(`${ux_specification_types_1.LogSeverityLabel[logEntry.severity]}: ${logEntry.message}, location: ${logEntry.location.path}`, ...meta);
            }
        }
        else {
            console.log(`${ux_specification_types_1.LogSeverityLabel[logEntry.severity]}: ${logEntry.message}`, ...meta);
        }
    }
};
exports.log = log;
//# sourceMappingURL=extensionLogger.js.map